{
  "name": "mini-crm-platform",
  "version": "1.0.0",
  "description": "Mini CRM Platform",
  "scripts": {
    "start": "cd backend && npm start",
    "build": "cd backend && npm install",
    "install-backend": "cd backend && npm install"
  },
  "engines": {
    "node": "18.x"
  }
}
